name = "forge-toefl-practice-new"
compatibility_date = "2026-08-28"
compatibility_flags = ["nodejs_compat"]
pages_build_output_dir = "dist"

[vars]
# Temporary review switch. Change to "false" and redeploy to restore Pro-only access.
PUBLIC_QUESTION_BANK_ACCESS = "true"
# AI requests are proxied to the Vercel production alias so Gemini runs from iad1.
VERCEL_AI_ORIGIN = "https://toeflwritingpractice.vercel.app"
